import React from 'react';

import FormPost from '../../templates/FormPost';

const NewPost = () => (
    <FormPost 
        currentTitle={''}
        currentCover={''}
        currentSlug={''}
        currentBody={''}
        currentDescription={''}
        currentKeywords={''}
    />
);

export default NewPost;
